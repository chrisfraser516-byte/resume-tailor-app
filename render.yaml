services:
  - type: web
    name: sharpen-resume-tailor
    runtime: node
    plan: starter
    buildCommand: echo "No build needed"
    startCommand: node server.js
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
